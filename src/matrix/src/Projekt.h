#ifndef _PROJEKT_H
#define _PROJEKT_H


#define PROJEKT_VERSION       0
#define PROJEKT_MINOR_VERSION 4

#include "ProjektMacros.h"
#include "ProjektTypes.h"

/*
extern const char *mitoa(int i,char *buff,int len);
extern double matod(const char *a);
extern int matoi(const char *a);
extern const char *mstrstr(const char *s1,const char *s2);

extern Bool IsNumber(const char* str);
extern Bool IsDouble(const char* str);
extern Bool IsName(const char* str);
*/

#endif // _PROJEKT_H
